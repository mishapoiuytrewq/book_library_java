spring.datasource.url=${PG_URL}
spring.datasource.username=${PG_USER}
spring.datasource.password=${PG_PASSWORD}

spring.flyway.url=${FLYWAY_PG_URL}
spring.flyway.user=${FLYWAY_PG_USER}
spring.flyway.password=${FLYWAY_PG_PASSWORD}
